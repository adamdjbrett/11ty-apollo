{
  "name": "apollo-zola-11ty-port",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:css && rm -rf _site && eleventy --config=eleventy.config.js --output=_site && pagefind --site _site --glob \"**/*.html\" && rm -rf src/public/pagefind && cp -R _site/pagefind src/public/pagefind",
    "start": "npm run build && eleventy --serve --config=eleventy.config.js --output=_site",
    "build:css": "node scripts/build-css.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.2",
    "@11ty/eleventy-plugin-rss": "^2.0.2",
    "luxon": "^3.5.0",
    "pagefind": "^1.3.0",
    "sass": "^1.86.3"
  }
}
